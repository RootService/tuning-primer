name: CI

on:
  push:
    branches: [ devel ]
  pull_request:

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Syntax check (POSIX sh)
        run: sh -n mysqltuner.sh

      - name: Help output
        run: ./mysqltuner.sh --help >/dev/null
